apiVersion: v1
kind: Service
metadata:
  name: {{ include "opensentinel.fullname" . }}
  labels:
    {{- include "opensentinel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opensentinel.selectorLabels" . | nindent 4 }}
